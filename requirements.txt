# Core reproduction pipeline (R7 — pinned for the environment we ran on).
torch>=2.1,<2.9
torchvision>=0.16
numpy>=1.24,<2.1
pandas>=2.0
Pillow>=9.5
scipy>=1.10
scikit-image>=0.20
scikit-learn>=1.2
matplotlib>=3.7
PyYAML>=6.0
tqdm>=4.65
medpy>=0.4.0          # HD95 (R3); pipeline degrades gracefully if absent
pytest>=7.4

# Optional — only for the nnU-Net comparison module (Section 8) and NIfTI I/O.
# nnunetv2>=2.2
# nibabel>=5.0
# SimpleITK>=2.2
